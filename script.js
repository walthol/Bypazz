        // HOW I CAN SET UP IF I FORGET!
        // 1. Go To "Game Buttons"
        // 2. Create The Button. The "button id" should be the same name as the name, but with the work "Button" at the end. same setup with capitals and crap like the others
        // 3. Go To "GAME URLS"
        // 4. Change what is typed in for the name already to your game id, and remove the word "Button"
        // 5. Put in the url for the game and add "," to the second to last line like the other ones // prefered to be from unbanned games or smth so it wont be blocked, like if its emulated ig
        // 6. Go to "button assign thing"
        // 7. After copying and pasting a line there to make my button assign thing, replace the "get element by id" to my game id
        // 8. On the far right replace the name to whatever i typed in for step 4
        // 9. using the button id and other thing just like the previous step, add the open notification.
        //
        // incase i forget, "window.open(urls.NAMEOFTHEAPPTHING, '_blank');" opens not in about:blank while "openGameInNewTab(urls.NAMEOFTHEAPPTHING);" does.
        //
        //
        //
        // also yes this file is pointless
